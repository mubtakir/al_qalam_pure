# 🖋️ دليل المستخدم لنظام القلم (Al-Qalam)

## 📖 ما هو القلم؟

**القلم** هو نظام ذكاء اصطناعي **عصبي-رمزي** (Neuro-Symbolic) يتميز بخاصية فريدة: **يكتب ذاته**.

بدلاً من تخزين المعرفة كأرقام غامضة (كما تفعل الشبكات العصبية التقليدية)، القلم يكتب معرفته ككود Python **قابل للقراءة والفهم والتعديل**.

> **الفكرة الجوهرية:** "الكود كأوزان" (Source Code as Weights)

---

## 🧠 لماذا القلم مختلف؟

| الأنظمة التقليدية (GPT, etc) | نظام القلم |
|------------------------------|------------|
| أوزان = أرقام غامضة | أوزان = كود مقروء |
| صندوق أسود | شفاف تماماً |
| لا يمكن تفسير قراراته | يمكن قراءة "تفكيره" |
| يحتاج إعادة تدريب للتعلم | يتعلم فوراً بكتابة كود جديد |
| قد يهلوس | منطق صارم قابل للتدقيق |

---

## 🏗️ البنية الأساسية

```
┌─────────────────────────────────────────────────┐
│              SelfWritingModel                   │
│           "النموذج الذي يكتب ذاته"                │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │           DynamicCell                   │   │
│  │         "الخلية الديناميكية"              │   │
│  │                                         │   │
│  │  - معرف (id): مثل "concept_fruit"       │   │
│  │  - نوع: concept/instance/rule/fact     │   │
│  │  - اتصالات بخلايا أخرى (بأوزان)         │   │
│  │  - to_source_code() → كود Python      │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
│  ┌─────────────┐  ┌─────────────┐             │
│  │ ImmuneSystem│  │ LLM_BRIDGE  │             │
│  │  "الحماية"   │  │"الدماغ المتبرع"│            │
│  └─────────────┘  └─────────────┘             │
│                                                 │
│  ┌─────────────────────────────────────────┐   │
│  │           LogicalBridge                 │   │
│  │      "جسر المنطق - لغة البيان"            │   │
│  └─────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

---

## ⚙️ كيف يعمل؟

### 1️⃣ التعلم

عندما تعلّم القلم مفهوماً جديداً:

```python
model.learn_concept("فاكهة", ["تفاح", "برتقال", "موز"])
```

**ما يحدث خلف الكواليس:**
1. ينشئ خلية مفهوم: `concept_fruit`
2. ينشئ خلايا أمثلة: `inst_apple`, `inst_orange`, `inst_banana`
3. يربط كل مثال بالمفهوم باتصال موزون (0.9)
4. **يكتب كل ذلك ككود Python** في `vault/cells.py`

**الكود المُولّد:**
```python
cell_concept_fruit = DynamicCell(
    cell_id="concept_fruit",
    cell_type="concept",
    metadata={"name": "فاكهة"}
)
cell_concept_fruit.connect_to('inst_apple', weight=0.9)
```

---

### 2️⃣ إضافة حقائق

```python
model.add_fact("التفاح", "لونه", "أحمر")
```

**النتيجة:**
- ينشئ اتصال بين خلية "التفاح" وخلية "أحمر"
- يسجل الحقيقة في المحرك المنطقي (Bayan)
- يمكن استخدامها في الاستدلال لاحقاً

---

### 3️⃣ الاستعلام

```python
print(model.query_knowledge("التفاح"))
```

**النتيجة:**
```
📌 التفاح (النوع: instance)
الحقائق: لونه أحمر
مرتبط بـ: فاكهة (0.9), أحمر (1.0)
```

---

### 4️⃣ الاستدلال

```python
model.infer({"topic": "فاكهة"})
```

**ما يحدث:**
- يطبق قواعد الاستدلال المخزنة
- يستنتج حقائق جديدة (مثلاً: "كل فاكهة طعام")
- إذا اكتشف قاعدة جديدة، **يكتبها ككود** ويحفظها

---

## 🛡️ نظام الحماية (ImmuneSystem)

**المشكلة:** ماذا لو أنتج القلم كوداً مكسوراً؟

**الحل:** قبل أي حفظ، يمر الكود عبر **اختبار sandbox**:

```python
# القلم يريد حفظ كود جديد
new_code = "def hello(): return 'مرحبا'"

# يختبره أولاً
result = model.immune.sandbox_test(new_code)

if result["valid"]:
    # الكود سليم → يحفظ ✅
else:
    # الكود مكسور → يرفض ❌
    # النظام لا يكسر نفسه أبداً
```

**المميزات:**
- ✅ يمنع أخطاء Syntax
- ✅ يكتشف الأخطاء Runtime
- ✅ يرفض الكود الخطير
- ✅ القلم لا يستطيع كسر نفسه

---

## 🧠 الدماغ المتبرع (LLM_BRIDGE)

يمكن ربط القلم بنموذج لغوي خارجي (Qwen/Llama) لتوليد الكود بذكاء:

```python
result = model.generate_code_with_llm("حساب القوة من الكتلة والتسارع")
```

**حلقة التصحيح الذاتي:**
```
1. LLM يولّد كود
       ↓
2. ImmuneSystem يختبره
       ↓
   ┌─ نجح ──→ يحفظ ✅
   │
   └─ فشل ──→ يرسل الخطأ للـ LLM
                    ↓
              يصحح ويعيد المحاولة
              (حتى 3 مرات)
```

---

## 📚 لغة البيان

القلم يدعم **لغة البيان** - لغة برمجة عربية لتعريف المفاهيم:

```bayan
مجال "الفيزياء":
{
    "معانٍ_أساسية": ["جسيم", "قوة", "طاقة"],
    "القوانين": ["قانون_نيوتن", "حفظ_الطاقة"]
}

كائن_وجودي "إلكترون" من_نوع "جسيم" في_مجال "الفيزياء":
{
    "خصائص_ذاتية": {
        "كتلة": 9.109e-31,
        "شحنة": -1.602e-19
    },
    "علاقات": {
        "يتفاعل_مع": ["بروتون", "فوتون"]
    }
}
```

**المجالات المتوفرة:**
- 🔬 الفيزياء (760 سطر)
- ⚗️ الكيمياء (737 سطر)
- 📐 الرياضيات (810 سطر)
- ⚡ الإلكترونيات (744 سطر)
- 🌱 علوم الحياة (356 سطر)

---

## 🔄 حلقة حياة القلم

```
      ┌──────────────────────────────────────────┐
      ▼                                          │
  [المستخدم يعلّم]                               │
      │                                          │
      ▼                                          │
  [القلم ينشئ خلايا]                             │
      │                                          │
      ▼                                          │
  [ImmuneSystem يتحقق] ──فشل──► [رفض + إصلاح]  │
      │                                          │
      │ نجح                                      │
      ▼                                          │
  [حفظ ككود في vault/]                           │
      │                                          │
      ▼                                          │
  [الاستدلال يولّد معرفة جديدة] ─────────────────┘
```

---

## 🚀 البدء السريع

### 1. إنشاء النموذج
```python
from core.self_writing_model import SelfWritingModel

model = SelfWritingModel(".")
```

### 2. تعليمه مفهوماً
```python
model.learn_concept("حيوان", ["قط", "كلب", "طائر"])
```

### 3. إضافة حقائق
```python
model.add_fact("القط", "يأكل", "السمك")
model.add_fact("الكلب", "صديق", "الإنسان")
```

### 4. الاستعلام
```python
print(model.query_knowledge("القط"))
```

### 5. رؤية الكود المُولّد
```bash
cat vault/auto_generated/cells.py
```

---

## 🎯 استخدامات القلم

| الاستخدام | الوصف |
|-----------|-------|
| **نظام خبير شفاف** | معرفة قابلة للقراءة والتعديل |
| **تعلم آلي تفسيري** | تفهم لماذا اتخذ قراراً |
| **نظام تعليمي** | يتعلم من الدروس مباشرة |
| **محرك معرفي** | يستدل ويستنتج حقائق جديدة |
| **واجهة عربية** | يدعم لغة البيان العربية |

---

## 📊 الحالة الحالية (V2.3)

| المكون | الحالة |
|--------|--------|
| ImmuneSystem | ✅ نشط |
| LogicalBridge | ✅ نشط |
| LLM_BRIDGE | ✅ متوفر |
| ملفات البيان | 8 ملفات |
| إجمالي الكود | ~3,600 سطر |

---

## ❓ الأسئلة الشائعة

**س: هل يحتاج GPU؟**
ج: لا، النواة تعمل على CPU. فقط LLM_BRIDGE قد يستفيد من GPU.

**س: هل يمكنه التعلم بدون LLM؟**
ج: نعم! `learn_concept()` و `add_fact()` لا تحتاج LLM.

**س: أين تُخزن المعرفة؟**
ج: في `vault/auto_generated/cells.py` ككود Python مقروء.

**س: هل يمكن تعديل المعرفة يدوياً؟**
ج: نعم! يمكنك تعديل الملفات مباشرة وإعادة تحميلها.

---

## 📞 للمساعدة

```bash
# اختبار النظام
python test_v23_integration.py

# عرض الخلايا المُخزنة
cat vault/auto_generated/cells.py
```

---

**V2.3 - Safe Sculpting** | 2026

> "القلم يكتب معرفته، ويحمي وجوده، ويتعلم باستمرار." 🖋️
